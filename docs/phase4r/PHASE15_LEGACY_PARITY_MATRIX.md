# Fase 15 — matriz de paridade completa com o legado

Status: **ACTIVE / MARCO C**

Corpus oficial: `Brasfoot.apk_Decompiler.com.zip` — SHA-256 `3eb5622ba9b5953a1bcc2c83c16700db86fc41c027989e34b8c00c207f25c465` — `com.brasfoot.v2020` — versionCode `202632`.

A matriz é incremental e não converte inferência em gameplay. `SMALI/executável` prevalece sobre Java decompilado em caso de divergência.

| LEGACY_FUNCTION | LEGACY_CLASS | JAVA_PATH | SMALI_PATH | CALL_PATH | REACHABILITY | INPUTS | RNG | MUTATIONS | PERSISTENCE | MODERN_EQUIVALENT | STATUS | EVIDENCE | TEST | ACTION |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `z()` | `best.m` | corpus `best/m.java` | corpus `best/m.smali` | `best.n.n()` → `best.b.d()` → `best.c0.l1()` → `best.m.z()` | reachable, annual flow | finance ledger instance | none | zeroes 10 periodic finance accumulators; preserves borrowed principal and monthly charge | serialized finance object in legacy; `career_club_manager_runtime` in modern | `LegacyFinanceLedgerRule.resetPeriod()` + Room transition boundary | `IMPLEMENTED_NEEDS_REVALIDATION` | Java+SMALI body; corpus-wide caller chain; `PHASE15_FINANCE_PERIOD_RESET_REACHABILITY.md` | `LegacyFinanceLedgerRuleTest`; public Room transition/reopen regression | certify exact Phase 15 FINAL_HEAD in CI |
| `l1()` | `best.c0` | corpus `best/c0.java` | corpus `best/c0.smali` | all clubs inside opening stage of `best.b.d()` | reachable | nullable finance object `Y` | none | calls `Y.z()` only when finance object exists | club finance object | iterate only already-materialized club runtime rows; no synthetic row | `IMPLEMENTED_NEEDS_REVALIDATION` | Java+SMALI null-check + invoke; lifecycle order characterization | public Room transition test includes two materialized clubs and one absent runtime | certify exact Phase 15 FINAL_HEAD in CI |
| `d()` (`NovoAno`) | `best.b` | corpus `best/b.java` | corpus `best/b.smali` | `ActivityFimAno.e()` → `best.n.n()` when `E1()` is true | reachable | global career/club/competition state and characterized flags | mixed; includes reachable randomness inside `best.n.m()` and nested annual selectors | multi-stage annual mutation | legacy career/save state | no whole-method modern equivalence claimed; certified finance reset and junior lifecycle are only promoted stages | `UNKNOWN_NEEDS_INVESTIGATION` | exact stage order in `PHASE15_ANNUAL_ROUTER_LIFECYCLE_EVIDENCE.md`; finance and junior evidence | finance public-path regressions + certified Phase 15.1 junior regressions | finish `best.n.m()` substantive callees and remaining stages before whole-lifecycle implementation |
| `n()` | `best.n` | corpus `best/n.java` | corpus `best/n.smali` | `ActivityFimAno.e()` sets `E1()` flag true before invoking `n()` | reachable | `best.b.E1()` route flag | none | no direct annual mutation; routes false → `best.n.i()`, true → `best.b.d()` | no independent persisted state | thin legacy router characterized; no new persistence needed | `CHARACTERIZED` | direct Java+SMALI branch equality in `PHASE15_ANNUAL_ROUTER_LIFECYCLE_EVIDENCE.md` | route itself requires no standalone state test; substantive callees remain separately gated | audit false-route `best.n.i()` under its own callers; annual true route continues through `best.b.d()` |
| `m()` | `best.n` | corpus `best/n.java` | corpus `best/n.smali` | `best.b.d()` stage 4 | reachable | original `P0()`, optional `o0/g0` queues, `V0`, `E1()` | one unconditional legacy `new Random().nextInt(100)` at this router; modern characterization makes this draw explicit through `RandomSource` without claiming APK seed parity | ordered dispatch to optional `d4`, gated `g4`, optional `e4`, unconditional `j2(1)`, optional `F2(true)`, then exact `V0/E1` final route | no new durable state proven by router alone | `LegacyAnnualNMRoutingRules` freezes control flow only; substantive callees remain separate | `IMPLEMENTED_NEEDS_REVALIDATION` | exact SMALI body frozen in `PHASE15_ANNUAL_NM_ROUTER_EVIDENCE.md` | `LegacyAnnualNMRoutingRulesTest` covers order, strict 50/51 boundary, unconditional draw, `P0` snapshot behavior and all final routes | certify new exact HEAD; then implement/classify `d4/e4/j2/F2/F` and compose runtime atomically |
| `d4()` | `best.b` | corpus Java cross-checked | corpus `best/b.smali` authoritative | optional first maintenance stage in `best.n.m()` when `o0()` is non-empty | reachable | legacy queue `I` / `components.o2`, current calendar | none in dispatcher body | for expired entries, conditionally invokes `best.o.U1(club)` then stages/removes the queue entry; per-entry exceptions are swallowed by caller boundary | legacy serialized queue; modern equivalent not yet proven | none yet | `REACHABLE_NOT_IMPLEMENTED` | SMALI loop/order characterized in `PHASE15_ANNUAL_NM_ROUTER_EVIDENCE.md` | pending `components.o2` + `best.o.U1` characterization | prove payload semantics and durable representation before schema/runtime change |
| `e4()` | `best.b` | corpus Java cross-checked | corpus `best/b.smali` authoritative | optional maintenance stage in `best.n.m()` after g4 gate when `g0()` is non-empty | reachable | legacy queue `J` / `components.y1`, current calendar | none in dispatcher body | invokes expired payload application then removes staged queue entries; caller swallows exceptions | legacy serialized queue; modern equivalent not yet proven | none yet | `REACHABLE_NOT_IMPLEMENTED` | SMALI loop/order characterized in `PHASE15_ANNUAL_NM_ROUTER_EVIDENCE.md` | pending `components.y1` payload characterization | prove payload fields/application and durable representation before implementation |
| `j2(1)` | `best.b` | corpus Java cross-checked | corpus `best/b.smali` authoritative | unconditional stage in `best.n.m()` after optional e4 | reachable | competition collection `G[0..d]`, each `best.a.x()` list | none | for each competition whose `x()` list is non-empty, calls `best.a.J(1)` | competition state; exact modern mapping not yet proven | none yet | `REACHABLE_NOT_IMPLEMENTED` | direct SMALI body; router order frozen in `PHASE15_ANNUAL_NM_ROUTER_EVIDENCE.md` | pending `best.a.J(1)` and competition-store equivalence tests | characterize `J(1)` before mutating modern competition state |
| `F2(true)` | `best.b` | corpus Java cross-checked | corpus `best/b.smali` authoritative | only when the original `P0()` snapshot at entry to `best.n.m()` equals zero | reachable | boolean true | none | writes legacy field `M0=true` | durable/transient role of `M0` not yet proven | none yet | `CHARACTERIZED` | direct one-field SMALI setter plus call condition in `best.n.m()` | router regression proves call placement; persistence test intentionally absent until `M0` role is proven | find all `M0` readers/writers before adding any modern field/schema |
| `F()` | `best.b` | corpus Java cross-checked | corpus `best/b.smali` authoritative | final route in `best.n.m()` whenever `E1()==true` | reachable | global competition/tournament/player collections | none in observed body | multi-pass reset: calls `k0.c(index)` across tournament collections, `o.d1(0)` over global players, then `o.D0()` over players from first `z0()` entry per competition | existing competition/player runtime may cover parts; exact equivalence not yet proven | none whole-method | `REACHABLE_NOT_IMPLEMENTED` | direct SMALI body; first-`z0()` access quirk recorded in `PHASE15_ANNUAL_NM_ROUTER_EVIDENCE.md` | pending callee characterization | audit `k0.c`, `o.d1`, `o.D0` and preserve first-entry quirk exactly |
| `g4()` → `n3` → `best.f` selection | `best.b` / `components.n3` / `best.f` | corpus Java cross-checked | corpus SMALI authoritative | reached only when `best.n.m()` gate is `>50` | reachable | players with `w()==true`, requested value `z0()`, candidate clubs | APK uses raw draws and implicit shuffle; branch already contains explicit deterministic compatibility primitives in `LegacyAnnualRandomRules` and pure predicates/ranges in `LegacyAnnualSelectionRules`, with no claim of APK seed parity | chooses candidate destination/value before T1; no mutation claimed until T1 | selection state is transient; no new durable state proven | pure RNG/selection characterization exists; object-level runtime composition still absent | `REACHABLE_NOT_IMPLEMENTED` | `PHASE15_ANNUAL_G4_TRANSFER_CHAIN.md` + re-audit in `PHASE15_ANNUAL_NM_ROUTER_EVIDENCE.md` | `LegacyAnnualRandomRulesTest`, `LegacyAnnualJAndBestFDeepTest` cover compatibility primitives; end-to-end selector pending | compose exact candidate collection/order with existing rules, then connect to certified T1 adapter; do not duplicate RNG policy |
| `T1(target,value,true,false,false)` annual `g4` mutation | `best.o` | corpus Java cross-checked | corpus `best/o.smali` authoritative | `g4()` → `n3` → `best.f` → selected club/value → `T1(...)` | reachable once selector returns destination | selected target/value plus player/source/destination state | none inside adapter; upstream selector owns randomness | mutates player club, rosters, 180-day contract, X/Y/Z/Q1 flags, slots and conditional finances | existing V14 runtime/commercial/transfer/membership/club-manager tables | `LegacyAnnualG4TransferExecutionRule` → `LegacyTransferExecutionRule` → `CareerManagerRuntimeStore.commitTransfer(...)` | `IMPLEMENTED_AND_CERTIFIED` | exact SMALI flags/effects in `PHASE15_ANNUAL_G4_TRANSFER_CHAIN.md`; included in exact certified head `75e2e082dbe5f1a03fcfb9a560f02a8f9dd1aee6` | `LegacyAnnualG4TransferExecutionRuleTest`; Phase 7 #864, Phase 8 #676, Final Certification #487 all SUCCESS on exact `75e2e082...` | later compose only after selector runtime is complete |
| annual entry `e()` plus remaining activity surface | `ActivityFimAno` | corpus `ActivityFimAno.java` | corpus `ActivityFimAno.smali` | `e()` → `best.b.z3(true)` → `best.n.n()` → `best.b.d()` | reachable | end-year UI/global state | no RNG in `e()` itself; callees may consume RNG | `e()` sets annual route flag and dispatches; other helpers include `j0.D0()/W0()` calls not yet classified | activity-local UI plus career effects through callees | annual entry characterized; whole activity not yet classified | `UNKNOWN_NEEDS_INVESTIGATION` | exact `e()` order in `PHASE15_ANNUAL_ROUTER_LIFECYCLE_EVIDENCE.md` | pending remaining helper/callee audit | audit `j0.D0()` / `j0.W0()` and separate presentation-only helpers from substantive effects |
| `y()` | `best.c0` | corpus `best/c0.java` | corpus `best/c0.smali` | callers include `best.a0`, `ActivityConvite`, `konrent.t`, `ActivityResults`, `ActivityMainTeam` | reachable | club/manager replacement state | deterministic path uses persisted career RNG where characterized | replacement-manager selection/attachment path | V11+ manager/coach runtime and persisted career RNG | manager replacement path promoted in Marco B / Fase 14 | `IMPLEMENTED_AND_CERTIFIED` | `MANAGER_METHOD_EVIDENCE.md` documents Java+SMALI characterization and persisted RNG promotion | replacement-manager and progression RNG tests certified in Marco B | Phase 15 only re-audits if new contradictory evidence appears |
| `h2(c0)` tryout generation path reached by junior activity | `best.b` | corpus `best/b.java` | corpus `best/b.smali` | `ActivityJuniores.j()` → availability checks → `best.b.h2(c0)` | reachable | cash, current junior count, six requested positions | shared career RNG; six gates interleaved with immediate `best.p.d(...)` draws | appends only generated pre-promotion drafts; raw finance code 9 expense | `career_junior_drafts` + `career_club_manager_runtime` + persisted `CareerState.random` | `CareerJuniorRuntimeStore.runTrial(...)` | `IMPLEMENTED_AND_CERTIFIED` | Java+SMALI characterization frozen in `LegacyJuniorRuntimeRules`; V14 separates `best.p` from materialized players | `CareerJuniorRuntimeStoreTest` covers deterministic stream, finance raw 9 and transactional rollback; exact-head CI `85cac622867779150695d7348379156691b79445` | no further Phase 15.1 action absent contradictory evidence |
| `b()` | `best.p` | corpus `best/p.java` unreliable for this method | corpus `best/p.smali` authoritative | annual junior maintenance while draft remains in youth list | reachable | age `C`, potential `N`, ability `O`, fractional accumulator `D` | none | for age ≤20 accumulates age/potential fractions; increments `O` only on strict `D > 1.0`; preserves age here | serialized `best.p`; modern `career_junior_drafts` V14 | `LegacyJuniorRuntimeRules.progressDevelopment(...)` + `CareerJuniorRuntimeStore.progressDevelopment(...)` | `IMPLEMENTED_AND_CERTIFIED` | SMALI-authoritative characterization frozen; no new sporting inference | `CareerJuniorDevelopmentRuntimeTest` covers `D == 1.0`, `D > 1.0`, age >20 and unrelated-field preservation; exact-head CI `85cac622867779150695d7348379156691b79445` | no further Phase 15.1 action absent contradictory evidence |
| `e(FALSE, p, c0)` manual promotion | `best.t` | corpus `best/t.java` | corpus `best/t.smali` | `ActivityJuniores.k()` → confirmation → `ActivityJuniores.i()` → `best.t.e(FALSE, p, c0)` | reachable | persisted `best.p`, target legacy club context, senior-list size | shared persisted career RNG; materialization starts only at this call | materializes final procedural player; manual FALSE removes draft immediately and stages final player on legacy D0 | V14 draft → procedural player/runtime/membership + persisted RNG | `CareerJuniorManualPromotionStore.promote(...)` | `IMPLEMENTED_AND_CERTIFIED` | Java+SMALI materialization; `PromotionRoute.MANUAL_FALSE`; no pre-promotion materialization | `CareerJuniorManualPromotionStoreTest` proves post-promotion draw-count ID, atomic commit and rollback after consumed RNG; exact-head CI `85cac622867779150695d7348379156691b79445` | no further Phase 15.1 action absent contradictory evidence |
| `c(c0)` + `e(TRUE,p,c0)` annual youth lifecycle | `best.p` + `best.t` + `best.b.q()` | corpus Java cross-checked | corpus `best/p.smali`, `best/t.smali`, `best/b.smali` authoritative | annual `best.b.q()` → original youth snapshot → `best.p.c(c0)` → optional `best.t.e(TRUE,p,c0)` | reachable | persisted draft, senior counts, club thresholds, annual flags/context | shared persisted career RNG; materialization and immediate replacement generation occur in proven order | increments age; promotes or refreshes; uses transient F1/E1/D1 staging; replacement generation immediate but insertion deferred; newly generated replacement not revisited in same cycle | final durable state represented by V14 draft table + procedural runtime/player/membership + persisted career RNG; F1/E1/D1 are not persisted | `LegacyJuniorAnnualLifecycleRules` + `CareerJuniorAnnualLifecycleStore.run(...)` | `IMPLEMENTED_AND_CERTIFIED` | direct SMALI characterization in `PHASE15_JUNIOR_ANNUAL_STAGING.md`; TRUE/FALSE share materialization while final list effects differ | `LegacyJuniorAnnualLifecycleRulesTest`, `CareerJuniorAnnualLifecycleStoreTest`, `CareerJuniorAnnualLifecycleReopenTest`; exact-head CI `85cac622867779150695d7348379156691b79445` | no further Phase 15.1 action absent contradictory evidence |
| youth-team activity surface (`onCreate`, `f/h/j/k/e/g/i/l`) | `ActivityJuniores` | corpus `ActivityJuniores.java` | corpus `ActivityJuniores.smali` | menu path from `ActivityMainTeam` | reachable | `best.p`, `best.c0`, `best.j0.O1[...]` and activity state | substantive RNG streams live in mapped callees | presentation/selection/validation/confirmation routes into the separately characterized trial, dismissal and manual-promotion seams; annual lifecycle is outside this Activity | activity-local UI state only; substantive durable state lives in the certified junior runtime boundaries | gameplay seams separately certified; remaining activity behavior is presentation/control for Phase 17 UI reconstruction | `PRESENTATION_ONLY` | complete Java+SMALI surface classification in `PHASE15_JUNIOR_ACTIVITY_SURFACE.md`; no additional gameplay seam found | substantive seams certified on exact head `85cac622867779150695d7348379156691b79445`; presentation itself deferred to UI milestone | reconstruct presentation faithfully in Phase 17; reopen only if contradictory gameplay evidence appears |

## Fase 15.1 — Juniores closure checkpoint

Fase 15.1 is **IMPLEMENTED_AND_CERTIFIED** on exact head `85cac622867779150695d7348379156691b79445` with:

- `Phase 7 Validation #853` — SUCCESS;
- `Phase 8 Validation #665` — SUCCESS;
- `Phase 8 Final Certification #476` — SUCCESS;
- PR #16 still Draft and mergeable with zero material review threads at certification time.

This closes the known reachable junior gameplay seams: tryout, dismissal, development, manual promotion, annual promotion/replacement, persisted RNG ordering, transactional rollback and save/reopen. It does **not** close Phase 15 / Marco C; the annual router/lifecycle gaps above remain active.

## Status rules

- `IMPLEMENTED_AND_CERTIFIED`: implementation and relevant behavior were already certified on an exact historical gate or, later, on the Phase 15 FINAL_HEAD.
- `IMPLEMENTED_NEEDS_REVALIDATION`: implementation exists but the current Phase 15 HEAD still needs its mandatory gate.
- `CHARACTERIZED`: behavior and reachability are proven, but no independent runtime implementation is necessary for the isolated seam or its substantive callees remain separately tracked.
- `REACHABLE_NOT_IMPLEMENTED`: executable behavior is proven reachable and the modern lifecycle has no equivalent.
- `UNREACHABLE`: no executable path after caller/callee audit.
- `PRESENTATION_ONLY`: behavior changes presentation only and carries no substantive gameplay mutation.
- `SMALI_REQUIRED`: Java is insufficient or ambiguous and executable evidence is still required.
- `UNKNOWN_NEEDS_INVESTIGATION`: evidence is incomplete; no gameplay is inferred.

The Fase 13 historical conclusion about `best.m.z()` having no caller is explicitly superseded by the Fase 15 corpus-wide chain documented in `PHASE15_FINANCE_PERIOD_RESET_REACHABILITY.md`; historical files are not rewritten.
